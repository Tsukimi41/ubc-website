import type { Metadata } from "next";
import { ArrowUpRight, BookOpen } from "lucide-react";
import { PageHero, PaperCard } from "@/components/ui";
import { getDiaryEntries } from "@/lib/cms";
import { formatJapaneseDate } from "@/lib/format";

export const metadata: Metadata = { title: "養蜂日誌", description: "屋上養蜂の日々と、Urban Bee Clubの研究記録。Notionから自動更新します。" };
export default async function DiaryPage() { const entries = await getDiaryEntries(); return <><PageHero eyebrow="Bee diary" title="巣箱をひらいた日の記録。" description="季節、群れの変化、技術の試行錯誤。長い時間をかけて見えてきたことを、記録として残します。"/><div className="page-shell py-16"><div className="mb-8 flex items-center gap-3 rounded-2xl bg-peach/30 p-4 text-sm text-bark/70"><BookOpen className="shrink-0 text-leaf"/><p>公開されたNotion「Bee Diary」と自動同期しています。</p></div><div className="grid gap-7 md:grid-cols-2 lg:grid-cols-3">{entries.map((entry) => <PaperCard key={entry.id} className="article-card"><div className="article-card-visual" aria-hidden="true">✎</div><div className="article-card-data"><div className="flex items-center justify-between text-xs"><span className="rounded-full bg-peach/60 px-3 py-1 font-bold">{entry.category}</span><time dateTime={entry.publishedAt} className="text-bark/55">{formatJapaneseDate(entry.publishedAt)}</time></div><h2 className="mt-5 text-xl font-black">{entry.title}</h2><p className="mt-3 line-clamp-5 text-sm leading-7 text-bark/70">{entry.excerpt}</p>{entry.url ? <a href={entry.url} target="_blank" rel="noopener noreferrer" className="mt-5 inline-flex items-center gap-1 font-bold text-[#9D4712] hover:underline">全文を読む <ArrowUpRight size={17}/></a> : <p className="mt-5 text-xs font-bold text-leaf">デモ記事</p>}</div></PaperCard>)}</div></div></>; }
